#include<iostream>

using namespace std;

int main(){

    int a, b, c, d;

    cin >> a >> b >> c >> d; // recebe os valores

    int dif = ((a*b) - (c*d)); // faz a equação necessária

    cout << "DIFERENCA = " << dif << endl; // imprime o resultado
    
    return 0;
}
